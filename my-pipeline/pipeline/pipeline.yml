name: nyc_taxi

schedule: "0 0 * * *"

start_date: "2022-01-01"

concurrency: 1
sequential: true

default_connections:
  duckdb: duckdb-default

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow", "green"]
